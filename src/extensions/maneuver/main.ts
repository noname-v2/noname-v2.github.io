import type { Extension } from '../extension';

export default <Extension>{
    cardpack: '军争',
    card: {
        hanbin: {
            name: '寒冰剑',
            content() {

            }
        }
    }
}