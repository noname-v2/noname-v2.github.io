import type { CollectionSGS } from '../sgs';

export const stage = <CollectionSGS>{
    before: {
        async content() {

        }
    },
    after: {
        async content() {
            
        }
    }
};